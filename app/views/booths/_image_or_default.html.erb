<% if booth.images.present? %>
  <%= link_to image_tag(booth.images.first.image.thumb("#{thumb}#").url), booth_path(booth.uid) %>
<% else %>
  <%= link_to image_tag("http://placehold.it/#{thumb}"), booth_path(booth.uid) %>
<% end %>