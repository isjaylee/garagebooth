<% if item.images.present? %>
  <%= link_to image_tag(item.images.first.image.thumb("540x500#").url), booth_item_path(@booth, item) %>
<% else %>
  <%= link_to image_tag("http://placehold.it/540x500"), booth_item_path(@booth, item) %>
<% end %>